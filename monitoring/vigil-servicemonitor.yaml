apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vigil-app
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - vigil-app
  selector:
    matchLabels:
      app: vigil-app
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
